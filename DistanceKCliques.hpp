#pragma once
#include "UndirectedGraph.h"

template <typename T>
void DistanceKCliques(const UndirectedGraph<T>& graph)
{
  std::cout << "DistanceKCliques on graph size " << graph.GetSize() << std::endl;
}